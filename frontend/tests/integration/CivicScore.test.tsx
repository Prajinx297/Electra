import { describe, it } from "vitest";

describe.skip("CivicScore integration", () => {
  it("increments score, unlocks badges, and restores persisted score once the Opus civic score UI lands", () => undefined);
});
