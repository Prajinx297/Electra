import { describe, it } from "vitest";

describe.skip("ElectionSimulator integration", () => {
  it("covers ballot ingestion, tallying, certification, and anomaly warnings once the Opus simulator lands", () => undefined);
});
