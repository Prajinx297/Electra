import { describe, it } from "vitest";

describe.skip("JourneyVisualizer integration", () => {
  it("renders DAG state nodes and dispatches node-click actions once the Opus visualizer lands", () => undefined);
});
